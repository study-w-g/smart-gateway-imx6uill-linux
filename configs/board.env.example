# 复制为 configs/board.env，并填写本机路径。
# 完成后的 board.env 会被 Git 忽略。

BOARD_NAME=imx6u-alpha-v2.2
ARCH=arm
CROSS_COMPILE=arm-linux-gnueabihf-

# 厂家 BSP 的本地目录，不要提交这些目录。
UBOOT_DIR=external/u-boot-atk
KERNEL_DIR=external/linux-atk
ROOTFS_DIR=external/buildroot-or-busybox

# 输出和部署参数
OUT_DIR=out/imx6u-alpha-v2.2
TFTP_DIR=/srv/tftp
NFS_ROOT=/srv/nfs/imx6u-alpha-v2.2
KERNEL_IMAGE=zImage
DTB_NAME=待根据厂家BSP确认.dtb

# 在开发板接线和启动介质确认前保持为空。
SERIAL_DEVICE=
TFTP_SERVER_IP=
BOARD_IP=
